# Thrive — 转盘拍摄（Spin）实现计划

> 一条生长记录从「一张照片」变成「一组可以拖着转的照片」。
> 不是 3D 重建 —— 是绕植物走一圈录段视频，按角度抽 24 帧，详情页拖动换帧。

---

## 1. 为什么不做真 3D

Apple 的 Object Capture 有两道硬门槛：引导拍摄要 LiDAR（12 Pro 及以后的 Pro 机型），
设备端出模型要 A17 Pro / M 系列（15 Pro 及以后）。我们的部署目标是 iOS 17 全机型，
上真 3D 等于绝大多数用户打开是灰的。

更要命的是被拍的东西。摄影测量对细薄、半透明、无纹理的表面就是失效的，
植物三样全占：叶片薄、透光、大片纯绿没有特征点，加上叶子会自己晃。
块根本身（不透明、有纹理、形状敦实）倒是理想目标，但只为块根做建模覆盖面太窄。

转盘是「多拍几个角度」这个想法里 90% 的体验、10% 的成本。

---

## 2. yaw 在这个场景下是可靠的

`MotionService` 里原本写着 yaw「磁北参考，室内会漂」，所以对齐只看 pitch / roll。
但实际用的是 `.xArbitraryZVertical`，这个参考系**不用磁力计**，yaw 是相对启动姿态的
积分值，漂移量级是每分钟一两度。

区别很重要：

- **跨天比 yaw** → 没意义（两次拍摄的 yaw 零点不同）。现状不用它是对的。
- **单次拍摄会话内比 yaw** → 可靠。绕一圈 20–30 秒，漂移零点几度，而帧间隔是 15°。

所以转盘可以完全靠 yaw 定位帧。

前提是**手机 yaw 变化量 = 绕植物转过的角度**，这只在「镜头始终对准植物」时成立
（原地转身也能转出 360° yaw，但那是全景不是转盘）。靠 UI 保证：转盘模式下强制
打开参考框，提示「让植物一直待在框里」。

---

## 3. 定下来的参数

| 项 | 取值 | 理由 |
|---|---|---|
| 帧数 | 24 帧 / 15° | 12 帧转起来能看出跳格；24→36 提升几乎感觉不到，文件却大 50% |
| 是否强制整圈 | 否，最少 8 帧 | 很多盆栽靠墙绕不了 360°，强制整圈会让一半场景直接用不了 |
| 帧规格 | 长边 1280px，HEIC，质量 0.7 | iPhone 390pt @3x = 1170px，1280 刚够；再降在详情页会糊 |
| 主图 | 2048px JPEG，**不动** | 封面、幽灵叠影、分享导出全靠它 |
| 视频副本 | 抽完即删 | 不留双份 |

抠图之后单帧约 130KB，一组约 3MB（不抠图不换 HEIC 是 9–10MB）。

---

## 4. 拍摄方式：绕着走，录视频抽帧

**绕着走**而不是把植物放转盘上：陀螺仪 yaw 只有手机移动时才有信号，物理转盘上
手机不动，`MotionService` 一点用都派不上。

**录视频抽帧**而不是连拍 24 张：

| | 连拍 24 张 | 录视频抽 24 帧 |
|---|---|---|
| 用户操作 | 走一圈，走太快会漏角度 | 按住录，走一圈，随便走 |
| 漏帧风险 | 有（每张 0.2–0.4 秒） | 没有（20 秒 = 600 帧可挑） |
| 单帧画质 | 高 | 4K 抽帧到 1280px 够用 |
| 运动模糊 | 无 | 有，但只在背景上 |

最后一行是关键：镜头始终对准植物，植物在画面中心基本静止，运动模糊主要糊在背景 ——
而背景本来就要抠掉。

**本版只做绕着走。** 原本设想「自动触发和手动快门共存」，让有支架和物理转盘的人
转一格点一下 —— 但那条路没有实现：转盘模式下快门只有开录 / 停录两态，
手机不动时 yaw 不变，覆盖角度恒为 0，必然被判定为没转够。

原地转盆要支持的话，得按**时间**等分抽帧而不是按 yaw，是另一个模式，本版不含。

---

## 5. 抠图是必做项，不是增强项

绕一圈背景从沙发转到窗户，转起来非常晃眼 —— 这是「绕着走」相对物理转盘的固有缺陷。

用 iOS 17 的 `VNGenerateForegroundInstanceMaskRequest` 把植物抠出来，**背景留透明**。
抠细叶子边缘同样不完美，但 2D 抠图糊一点几乎看不出来，比 3D 重建宽容得多。

留透明而不是合到纯色底：详情页浅色和深色模式底色不同，透明能跟着主题走，
写死一个底色总有一边难看。实测 HEIC 能保住透明通道。

**降级是整组的，不是单帧的。** 只要有一帧没抠出主体，整组都退回原图 ——
23 帧干净背景里混一帧带房间的，转到那儿会闪一下，比整组都留着背景更像 bug。
（实测：图里没有明确主体时 Vision 返回空结果，降级判断能触发。）

体积方面别指望太多。抠掉杂乱背景确实省，但 alpha 通道本身要占字节：
在背景本来就干净的素材上实测反而涨了一倍。真实照片背景是杂乱的，
净下来应该还是省的，但「单帧 130KB」是估的，等真机拍了才算数。

---

## 6. 数据模型

```
GrowthEntry
  photoFilename: String        ← 不动，就是转盘第 0 帧（2048 JPEG）
  spinFilenames: [String]?     ← 新增，24 个 HEIC 文件名，按角度顺序
  spinStartYaw: Double?        ← 新增，第 0 帧的 yaw；帧间隔固定 15°，够还原每帧角度
```

`photoFilename` 保持单张语义是整个方案能低成本落地的关键 —— 时间轴、封面、
幽灵叠影、导出、删除逻辑一行都不用改，转盘只是详情页多一个能转的视图。

新增字段全是 optional，走 SwiftData 轻量迁移。

不为帧建独立 `@Model`：帧没有独立生命周期，跟着 entry 生死，`[String]` 就够。

---

## 7. 各页面的成品形态

- **时间轴**：跟现在一模一样，还是一张竖图。只在角上加个小图标表示「这条能转」。
  转盘不该把时间轴变成一堆会动的东西。
- **详情页**：打开是静态的第 0 帧，手指左右拖才开始转。不拖它就是张照片。
- **分享**：系统分享面板给不了「能转的东西」，只能分享主图单张。
  想让别人看到转的效果得额外做「导出 2 秒循环视频」—— 那是独立工作量，本计划不含。

### 跟真 3D 的差距（要有预期）

只有一圈水平视角，没有俯视仰视；换不了光照、量不了体积、放大看不了细节；
帧之间是跳变不是插值。

---

## 8. 落地步骤

```
1. 数据模型 + 存储层
   GrowthEntry 加 spinFilenames / spinStartYaw
   PhotoStore 加一组帧的 HEIC 存 / 读 / 删
   → 验证：装旧版数据的包能升级不丢；写入 24 帧再读回一致；
           删记录时帧跟着删干净

2. 录制 + 抽帧
   CameraService 加视频录制，开录前锁 exposureMode 和 whiteBalanceMode
   录制期间按时间戳采 yaw 序列，录完按角度匹配 presentation time 抽帧
   → 验证：真机绕一圈出 24 个文件；相邻帧角度差 15°±3°；
           首尾帧亮度差肉眼看不出

3. 抠图
   24 帧过 VNGenerateForegroundInstanceMaskRequest，背景留透明
   有任何一帧抠不出主体，整组退回原图
   → 验证：一组处理耗时可接受；叶子边缘无明显豁口（要真机真照片）

4. 拍摄页转盘模式
   进入模式强制打开参考框，提示「让植物一直待在框里」
   进度环显示已覆盖角度，随时可停
   → 验证：半圈（12 帧）能正常保存；不足 8 帧时给提示而不是存一半

5. 详情页 spin 播放器
   拖拽换帧，播放器自己持有帧数组，不走 PhotoStore 的共享 NSCache
   → 验证：拖动不掉帧；进出详情页十次内存不涨
```

第 1 步独立可先做。2 依赖 1，4 依赖 2，5 依赖 1。3 可拿静态图先开发，不必等 2。

---

## 9. 三个坑

**抽帧要对齐时间戳，这是第 2 步真正的难点。**
`CMDeviceMotion` 的采样和视频帧是两条独立时间线，得记下每个 yaw 采样的时刻，
再拿目标角度反查时刻喂给 `AVAssetImageGenerator`。做不好就是角度间隔忽大忽小。

**保存不再是瞬时的。**
抠 24 帧要几秒。先把原帧存下、记录立刻入库，抠图放后台跑完再替换，用户不用等。
这是视频方案带进来的新 UX 问题。

**pitch 要在转盘模式下继续管住。**
它锁的是举手机的高度，跨时间两组转盘能不能对上全看它。
现有的 `PoseIndicatorBar` 直接复用。

---

## 10. 顺带存下来的东西

帧间隔固定、第 0 帧仍用 pitch/roll 对齐，两次拍摄的第 n 帧天然对应大致相同的角度。
以后可以做「两个时间点的转盘并排同步旋转」，对「看变化」的说服力比单张对比强得多。

本版不做，但 `spinStartYaw` 存下来就够未来回溯 —— 跟当初存 pose 字段是同一个思路。
